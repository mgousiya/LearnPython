# PYTHON OOPS 
# object: A "bundle of related attributes(variables) and methods(functions)"
# Ex: phone,cup,book
#Note: A method is a function that belongs with an object

# you need a "class" to create many objects 
# class = "Blueprint" used to design the structure and layout of an object.

#__init__(): method is a special method (also known as a constructor) 
#             used for initializing a newly created object
#Self: It is used to access instance variables (attributes) and other methods within the same class.




class Car:
    def __init__(self,model,year,color,for_sale):
        self.model = model
        self.year = year
        self.color = color
        self.for_sale = for_sale

car1 = Car("Benz","2024","black",False)
car2 = Car("Tata","2023","red",True)
car3 = Car("BMW","2022","white",True)

print(car3.model)
print(car3.year)
print(car3.color)
print(car3.for_sale)